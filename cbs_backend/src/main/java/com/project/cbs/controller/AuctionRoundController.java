package com.project.cbs.controller;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/rounds")
@CrossOrigin(origins = "*")
public class AuctionRoundController {

    // TODO: Implement auction round endpoints
    // - GET /api/rounds - Get all rounds
    // - GET /api/rounds/{id} - Get round by ID
    // - GET /api/rounds/current - Get current round
    // - POST /api/rounds - Create new round
    // - PUT /api/rounds/{id}/close - Close round
    // - POST /api/rounds/{id}/process - Process round results
}
